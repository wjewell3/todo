<%= form_for @changeset, topic_path(@conn, :create) fn f -> %>
  <div class="form-group">
    <%= text_input f, :action, placeholder: "Action", class: "form-control" %>
    <%= error_tag f, :action  %>
  </div>

  <%= submit "Save ToDo", class: "btn btn-primary" %>
<% end %>